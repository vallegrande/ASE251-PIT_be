spring:
  application:
    name: camote-riesgos

  datasource:
    url: jdbc:sqlserver://localhost:1433;databaseName=camote_db;encrypt=false;trustServerCertificate=true
    username: sa
    password: CamoteDb!2026
    driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver

  sql:
    init:
      mode: always
      schema-locations: classpath:sql/db.sql

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.SQLServerDialect
        format_sql: true

server:
  port: 8085